<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Referencia al SDK UniPRT de TSC (paquete local) -->
    <PackageReference Include="UniPRT.Sdk" Version="2.0.0.3" />
  </ItemGroup>

</Project>
